# jsonrpcbase

Simple JSON-RPC service without transport layer.

This repository includes a vendored copy of [jsonrpcbase](https://github.com/kbaseincubator/jsonrpcbase) to resolve dependency conflicts. The original repository was only used by this project, so the code has been brought in directly to simplify maintenance and eliminate external dependency issues.